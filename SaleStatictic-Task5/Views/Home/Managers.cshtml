@model System.Collections.Generic.List<SaleStatictic_Task5.Models.ManagerViewModel>

<div id="managers">
    <table class="table table-striped">
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ManagerName</td>
                <td>@item.Id</td>
            </tr>
        }
    </table>

</div>